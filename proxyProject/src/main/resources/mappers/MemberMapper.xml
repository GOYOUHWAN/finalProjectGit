<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MemberMapper">
	
	<insert id="memberJoin" parameterType="com.choa.member.MemberDTO">
		insert into member
		values(#{id},#{pw},#{name},#{age})

	</insert>


	<select id="memberLogin" resultType="com.choa.member.MemberDTO" parameterType="com.choa.member.MemberDTO">
		select * from member where id=#{id} and pw=#{pw}
	</select>
	
	<update id="memberUpdate" parameterType="com.choa.member.MemberDTO">
		update member 
		<trim prefix="set" suffixOverrides=",">
			<if test="pw != null">
				pw = #{pw}
			</if>
			<if test="age != 0">
				age = #{age}
			</if>

		
		</trim>
		where num=#{num}
	</update>

	<delete id="memberDelete" parameterType="com.choa.member.MemberDTO">
		<!-- delete member where num=#{num} -->
	</delete>


</mapper>